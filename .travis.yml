dist: trusty
sudo: true
language: python
python:
  - "3.6"

install:
  - pip install --upgrade pip
  - pip install pipenv
  - pipenv install --system --skip-lock
  - pip install -r requirements.txt

script: 
  - sudo python app.py

