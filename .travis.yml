dist: trusty
sudo: required 
language: python
python:
  - "3.6"

install:
  - pip install --upgrade pip
  - pip install pipenv
  - pipenv install --system --skip-lock

script: 
  - sudo python app.py

