dist: trusty
sudo: true
language: python
python:
  - "3.6"

install:
  - pip install --upgrade pip
  - pip install pipenv
  - pipenv install
  - pipenv shell
  - pip install -r requirements.txt

script: 
  - sudo python3 app.py

